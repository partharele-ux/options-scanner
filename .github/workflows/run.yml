name: Run Scanner

on:
  schedule:
    - cron: "*/5 4-10 * * 1-5"   # Every 5 min (IST market hours)
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install requests pytz

      - name: Run scanner
        run: python scanner.py
